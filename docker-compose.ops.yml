services:
  mcp:
    build:
      target: mcp-ops-runtime
    environment:
      ENABLE_OPS_TOOLS: "true"
      OPS_ALLOWLIST_PATH: /app/ops-allowlist.json
    volumes:
      - ./ops-allowlist.json:/app/ops-allowlist.json:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - /srv:/srv
